server:
  port: 0
  servlet:
    contextPath: /order-management
spring:
  application:
    name: order-management
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: dmadhupada@gmail.com
    password: 6305512570@d
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
